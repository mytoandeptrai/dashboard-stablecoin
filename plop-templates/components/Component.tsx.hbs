import type { FCC } from '@/types';

type {{pascalCase name}}Props = {
  //
};

const {{pascalCase name}}: FCC<{{pascalCase name}}Props> = ({children}) => {
  return <>{{pascalCase name}}</>;
};

export default {{pascalCase name}};
